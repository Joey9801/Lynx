#include "debug.c"
#include "ecc.c"
#include "packets.c"
#include "setup.c"


int main (void){

	//setup the clock
	//setup the serial port
	//setup the rest of the peripherals

	//setup the status leds

	//configure the pll
	//wait for pll lock

	//setup all of the timers

	//enable interrupts globally
	for ever {
		while(!todo) //just chill while there isn't anything to do
		int i;
		for(i=0; i<=5; i++){
			if((todo>>i)&1)
				break;
		
	}
}
